# optional custom contents for /proc/cmdline (non-gki) or
# /proc/bootconfig (gki). The built-in boot script generates and applies a
# fresh sanitized snapshot without appending it here.
# Leave empty/untouched to use only the generated snapshot.
#
# populate by dumping the real file first, then editing the fields you
# want spoofed, e.g. for /proc/bootconfig style:
#   cat /proc/bootconfig > cmdline_or_bootconfig.txt
#   then edit lines like:
#	 androidboot.bootreason = "reboot"
#	 androidboot.vbmeta.device_state = "locked"
#	 androidboot.verifiedbootstate = "green"
#   and delete androidboot.verifiedbooterror / androidboot.verifyerrorpart lines
#
# or for /proc/cmdline style:
#   cat /proc/cmdline > cmdline_or_bootconfig.txt
#   then edit inline, e.g.:
#	 androidboot.verifiedbootstate=green androidboot.vbmeta.device_state=locked
#
# note: this spoofing has limited real-world effect since no known root
# detector currently checks it, but the mechanism is here if needed

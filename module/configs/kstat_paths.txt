# one path per line, spoofed via add_sus_kstat -> update_sus_kstat
# staged early (post-fs-data, BEFORE the path gets bind mounted/overlaid)
# committed late (boot-completed, AFTER the mount happens) to fix up the
# changed ino/dev while keeping size/blocks consistent with the pre-mount stat
# use update_sus_kstat_full_clone instead if you want the full stat cloned
#
# Only add a path after confirming its stat identity needs correction. Module
# mounts such as hosts are handled by targeted KernelSU umount registration and
# should not receive a guessed Kstat record.
